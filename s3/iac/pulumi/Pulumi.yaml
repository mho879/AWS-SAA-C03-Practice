name: s3-simple-sdk
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A Python project for AWS S3 bucket creation
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
